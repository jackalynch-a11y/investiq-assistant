{
  "name": "investiq-assistant",
  "version": "1.0.0",
  "description": "AI-powered investment assistant for company reviews",
  "private": true,
  "engines": {
    "node": "18.0.0"
  },
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "backend": "npm run dev --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend && npm run build --prefix backend",
    "start": "npm start --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "keywords": [
    "investment",
    "ai",
    "company-review",
    "assistant"
  ],
  "author": "InvestIQ",
  "license": "MIT"
}
